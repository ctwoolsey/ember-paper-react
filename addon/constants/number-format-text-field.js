import { BASE_EMBER_COMPONENT_PREFIX } from './constants';

const IDS = {
  NUMBER_FORMAT_TEXT_FIELD: `NumberFormatTextField`
}

const NUMBER_FORMAT_TEXT_FIELD = {
  ID: IDS.NUMBER_FORMAT_TEXT_FIELD,
  COMPONENT_TYPE: `${BASE_EMBER_COMPONENT_PREFIX}-number-format-text-field`,
}

export {
  NUMBER_FORMAT_TEXT_FIELD
}
