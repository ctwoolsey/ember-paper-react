import { REACT_COMPONENT_PREFIX, BASE_EMBER_COMPONENT_PREFIX } from "./constants";

const IDS = {
  FAB: `${REACT_COMPONENT_PREFIX}Fab`
}

const FAB = {
  ID: IDS.FAB,
  COMPONENT_TYPE: `${BASE_EMBER_COMPONENT_PREFIX}-fab`,
}

export {
  FAB
}
