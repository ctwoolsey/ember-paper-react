import { REACT_COMPONENT_PREFIX, BASE_EMBER_COMPONENT_PREFIX } from './constants';

const IDS = {
  TAB: `${REACT_COMPONENT_PREFIX}Tab`
}

const TAB = {
  ID: IDS.TAB,
  COMPONENT_TYPE: `${BASE_EMBER_COMPONENT_PREFIX}-tab`,
}

export {
  TAB
}
