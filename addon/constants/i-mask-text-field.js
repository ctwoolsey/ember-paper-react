import { BASE_EMBER_COMPONENT_PREFIX } from './constants';

const IDS = {
  I_MASK_TEXT_FIELD: `IMaskTextField`
}

const I_MASK_TEXT_FIELD = {
  ID: IDS.I_MASK_TEXT_FIELD,
  COMPONENT_TYPE: `${BASE_EMBER_COMPONENT_PREFIX}-i-mask-text-field`,
}

export {
  I_MASK_TEXT_FIELD
}
