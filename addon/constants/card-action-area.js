import { REACT_COMPONENT_PREFIX, BASE_EMBER_COMPONENT_PREFIX } from "./constants";

const IDS = {
  CARD_ACTION_AREA: `${REACT_COMPONENT_PREFIX}CardActionArea`
}

const CARD_ACTION_AREA = {
  ID: IDS.CARD_ACTION_AREA,
  COMPONENT_TYPE: `${BASE_EMBER_COMPONENT_PREFIX}-card-action-area`,
}

export {
  CARD_ACTION_AREA
}
