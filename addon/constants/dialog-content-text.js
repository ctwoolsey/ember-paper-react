import { REACT_COMPONENT_PREFIX, BASE_EMBER_COMPONENT_PREFIX } from "./constants";

const IDS = {
  DIALOG_CONTENT_TEXT: `${REACT_COMPONENT_PREFIX}DialogContentText`
}

const DIALOG_CONTENT_TEXT = {
  ID: IDS.DIALOG_CONTENT_TEXT,
  COMPONENT_TYPE: `${BASE_EMBER_COMPONENT_PREFIX}-dialog-content-text`,
}

export {
  DIALOG_CONTENT_TEXT
}
