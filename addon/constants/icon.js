import { REACT_COMPONENT_PREFIX, BASE_EMBER_COMPONENT_PREFIX } from "./constants";

const IDS = {
  ICON: `${REACT_COMPONENT_PREFIX}Icon`
}

const ICON = {
  ID: IDS.ICON,
  COMPONENT_TYPE: `${BASE_EMBER_COMPONENT_PREFIX}-icon`,
  SVG_PATH: `PATH`,
  SVG_D: `d`
}

export {
  ICON
}
