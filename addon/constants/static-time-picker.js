import { REACT_COMPONENT_PREFIX, BASE_EMBER_COMPONENT_PREFIX } from './constants';

const IDS = {
  STATIC_TIME_PICKER: `${REACT_COMPONENT_PREFIX}StaticTimePicker`
}

const STATIC_TIME_PICKER = {
  ID: IDS.STATIC_TIME_PICKER,
  COMPONENT_TYPE: `${BASE_EMBER_COMPONENT_PREFIX}-static-time-picker`,
}

export {
  STATIC_TIME_PICKER
}
