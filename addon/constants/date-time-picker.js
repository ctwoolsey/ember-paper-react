import { REACT_COMPONENT_PREFIX, BASE_EMBER_COMPONENT_PREFIX } from './constants';

const IDS = {
  DATE_TIME_PICKER: `${REACT_COMPONENT_PREFIX}DateTimePicker`
}

const DATE_TIME_PICKER = {
  ID: IDS.DATE_TIME_PICKER,
  COMPONENT_TYPE: `${BASE_EMBER_COMPONENT_PREFIX}-date-time-picker`,
}

export {
  DATE_TIME_PICKER
}
