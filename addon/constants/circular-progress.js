import { REACT_COMPONENT_PREFIX, BASE_EMBER_COMPONENT_PREFIX } from "./constants";

const IDS = {
  CIRCULAR_PROGRESS: `${REACT_COMPONENT_PREFIX}CircularProgress`
}

const CIRCULAR_PROGRESS = {
  ID: IDS.CIRCULAR_PROGRESS,
  COMPONENT_TYPE: `${BASE_EMBER_COMPONENT_PREFIX}-circular-progress`,
}

export {
  CIRCULAR_PROGRESS
}
