import { REACT_COMPONENT_PREFIX, BASE_EMBER_COMPONENT_PREFIX } from "./constants";
import { AVATAR } from "./avatar";

const IDS = {
  AVATAR_GROUP: `${REACT_COMPONENT_PREFIX}AvatarGroup`
}

const AVATAR_GROUP = {
  ID: IDS.AVATAR_GROUP,
  COMPONENT_TYPE: `${BASE_EMBER_COMPONENT_PREFIX}-avatar-group`
}

export {
  AVATAR_GROUP
}
