<span class={{this.componentType}} ...attributes
  {{ember-paper-reactable this.inserted this.changeReactState this.changeArgs}}
>
  {{#if (has-block 'avatar')}}
    <AttributeNodeChild class="react=component avatar" @attribute="avatar" @loadAttributeInfo={{this.loadAttributeInfo}}>{{yield to='avatar'}}</AttributeNodeChild>
  {{/if}}
  {{#if (has-block 'action')}}
    <AttributeNodeChild class="react=component action" @attribute="action" @loadAttributeInfo={{this.loadAttributeInfo}}>{{yield to='action'}}</AttributeNodeChild>
  {{/if}}
  {{#if (has-block 'subheader')}}
    <AttributeNodeChild class="react=component subheader" @attribute="subheader" @loadAttributeInfo={{this.loadAttributeInfo}}>{{yield to='subheader'}}</AttributeNodeChild>
  {{/if}}
  {{#if (has-block 'title')}}
    <AttributeNodeChild class="react=component title" @attribute="title" @loadAttributeInfo={{this.loadAttributeInfo}}>{{yield to='title'}}</AttributeNodeChild>
  {{/if}}
</span>